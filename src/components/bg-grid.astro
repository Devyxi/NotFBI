<div style="
position: fixed;
top: 0; right: 0; bottom: 0; left: 0;
z-index: -10;
height: 100vh;
width: 100%;
background-size: 4rem 4rem;
background-image:
	linear-gradient(to right, #222, 1px, transparent 1px),
	linear-gradient(#222, 1px, transparent 1px);
-webkit-mask-image: radial-gradient(ellipse 80% 100% at 50% 0%, white 60%, transparent 100%);
mask-image: radial-gradient(ellipse 80% 100% at 50% 0%, white 60%, transparent 100%);
pointer-events: none;
"></div>